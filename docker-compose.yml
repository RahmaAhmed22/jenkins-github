services:
  mysql:
    image: mariadb:latest
    container_name: db_mod
    env_file: "database.env"
    volumes:
      - maria-data:/var/lib/backup/data

  php:
    build: .
    ports:
      - "3001:80"

volumes:
   maria-data:
